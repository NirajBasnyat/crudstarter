<div class="form-group">
    <label for="{{$id}}">{{$label}}</label>
    <textarea class="{{$class ?? 'form-control'}}" name="{{$name}}" id="{{$id}}" rows="{{$rows}}" cols="{{$cols}}">{{$value}}</textarea>
</div>